# WeightDecomp
Weight decomposition

Conclusion: Does not work! Initialization scale matters more.

## Installation

```bash
# Clone the repository
git clone <repo-url>
cd WeightDecomp

# venv
python -m venv .venv

# activate
source .venv/bin/activate

# Install dependencies
pip install -e .
```
